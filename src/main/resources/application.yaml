spring:
  application:
    name: selective-tests
  flyway:
   default-schema: selective
   validate-on-migrate: false
  datasource:
    url: jdbc:postgresql://localhost:5432/mydatabase
    username: myuser
    password: secret
    hikari:
      schema: selective
  session:
    jdbc:
      initialize-schema: never
      table-name: selective.spring_session
